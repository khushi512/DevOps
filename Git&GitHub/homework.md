### Difference

`git commit -m` commits changes that have been staged using `git add`.

`git commit -a -m` automatically stages changes to already tracked files and commits them. It does not include new untracked files.

# Git & GitHub Homework

## Task 1: git commit -m vs git commit -a -m

### git commit -m
Testing git commit -a -m
Main branch commit 2
Main branch commit 3
Feature change 1
